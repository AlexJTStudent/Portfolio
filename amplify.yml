version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g quarto-cli
    build:
      commands:
        - quarto render
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'